"""Small thumbnail readability — large shapes, clean silhouette."""

from __future__ import annotations


def sticker_readability_positive() -> str:
    return (
        "large expression shapes, simple readable acting, single clear emotion, "
        "clean silhouette, thick outer shape readability, instant emotion at thumbnail size, "
        "bold sticker pose readable at 72px"
    )


def sticker_readability_negative() -> str:
    return (
        "micro details, thin lines, tiny mouth changes, subtle expression, "
        "fine hair strands, complex background clutter, small illegible gesture, "
        "passport photo calm, symmetrical bust only"
    )
